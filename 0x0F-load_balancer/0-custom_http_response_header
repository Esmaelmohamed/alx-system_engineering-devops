cript to customize a 404 error page in Nginx

# Update package lists and install Nginx
sudo apt-get update
sudo apt-get install -y nginx

# Create a basic index.html page
echo "Hello World!" | sudo tee /var/www/html/index.html

# Set up a permanent redirection to a GitHub repository
redirection_config="server_name _;\n\trewrite ^\/redirect_me https:\/\/github.com\/Esmaelmohamed/alx-system_engineering-devops permanent;"
sudo sed -i "s/server_name _;/$redirection_config/" /etc/nginx/sites-enabled/default

# Create a custom 404 error page with a message
echo "Ceci n'est pas une page" | sudo tee /var/www/html/404.html

# Configure Nginx to use the custom 404 error page and add an HTTP response header
nginx_config="listen 80 default_server;\n\terror_page 404 \/404.html;\n\tlocation = \/404.html {\n\t\troot \/var\/www\/html;\n\t\tinternal;\n\t}\n\n# Add HTTP response header\nserver_name _;\n\tadd_header X-Served-By $HOSTNAME;"
sudo sed -i "/listen 80 default_server;/c $nginx_config" /etc/nginx/sites-enabled/default

# Test Nginx configurations for syntax errors
sudo nginx -t

# Restart Nginx to apply the changes
sudo service nginx restart

